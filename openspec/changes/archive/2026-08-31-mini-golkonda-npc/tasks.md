## 1. Visual Update

- [x] 1.1 Update the `displayId` of NPC 39900 to `25126` in `dist/game/data/stats/npcs/custom/custom.xml`.
- [x] 1.2 Update the `radius` to `10` and `height` to `22` in the same NPC block to scale the model down.
