## 1. Data Structures
- [x] 1.1 Criar a classe `EconomyProfile` e `EconomyItem` para suportar os dados em memória.

## 2. Parsers XML
- [x] 2.1 Criar a classe `FakeTradersEconomyParser`. Implementar a leitura do DOM e carregamento da hashmap de profiles.
- [x] 2.2 Criar a classe `FakeTradersSpawnParser`. Implementar a leitura do DOM, sorteio de nomes, instanciação e chamada do `spawnMe()`.

## 3. Server Boot Injection
- [x] 3.1 Adicionar a inicialização dos Parsers no `GameServer.java`.
